# AI Agent Instructions

> **CRITICAL INSTRUCTION FOR ALL AI CODING AGENTS (DEVIN, CLAUDE, ETC.)**

Before you begin exploring this repository or attempting to implement any task, you **MUST** adhere to the following rules:

1. **READ FIRST**: Read `docs/AGENT_HANDOFF.md` immediately. 
2. **CANONICAL STATE**: Treat `docs/AGENT_HANDOFF.md` as the absolute canonical engineering state snapshot of this project.
3. **DIRECT NAVIGATION**: Use the file paths, architecture maps, and component descriptions in the handoff document to navigate directly to the relevant code.
4. **AVOID BROAD SCANS**: Do NOT scan, grep, or list the entire repository structure unless the task genuinely requires it. Save your context window and tokens.
5. **PRESERVE CONSTRAINTS**: Strictly preserve the architectural decisions, constraints, and "do not touch" areas documented in the handoff file.
6. **UPDATE ON CHANGE**: You are responsible for keeping the handoff document current. Update `docs/AGENT_HANDOFF.md` in the same commit whenever you alter the implementation state, add a new feature, encounter a known issue, or record a failed approach.
